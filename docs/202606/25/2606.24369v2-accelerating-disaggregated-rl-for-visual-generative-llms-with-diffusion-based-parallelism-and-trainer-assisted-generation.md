---
title: Accelerating Disaggregated RL for Visual Generative LLMs with Diffusion-Based Parallelism and Trainer-Assisted Generation
title_zh: 基于扩散并行与训练器辅助生成的分布式RL加速视觉生成大语言模型
authors: "Sijie Wang, Zhengyu Qing, Zhiqiang Tan, Yiming Yin, Yeqing Zhang, Yaoyuan Wang, Qiang Wang, Xiaowen Chu, Shaohuai Shi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24369v2"
tags: ["query:image-gen-rl"]
score: 9.0
evidence: 直接将强化学习应用于扩散图像生成
tldr: 针对扩散生成式大语言模型的强化学习系统存在共置执行导致资源耦合、扩展受限的问题。本文提出DigenRL，一种分离式RL框架，通过生成轴流水线（GAP）、时间步并行（TSP）、弹性训练器辅助生成（TAG）及紧耦合一步约束异步策略实现细粒度流水线。在多种模型和16-32 GPU上吞吐量提升1.56-2.10倍，支持异构GPU和灵活资源调度。
source: arxiv
selection_source: fresh_fetch
motivation: 现有扩散RL系统采用共置执行，简化同步但限制资源独立扩展与异构部署。
method: 提出生成轴流水线、时间步并行、弹性训练器辅助生成及紧耦合一步约束异步策略。
result: 在HunyuanVideo-13B等模型和16-32 GPU上，吞吐量较veRL-Omni和GenRL提升1.56-2.10倍。
conclusion: DigenRL验证了分离式架构在扩散RL中的有效性，实现灵活资源分配与高效调度。
---

## 摘要
强化学习（RL）已成为主导的后训练范式，推动了面向自回归大语言模型（LLMs）的高性能RL系统（如veRL）的出现。与此同时，面向扩散的RL算法（如DanceGRPO和FlowGRPO）迅速将RL的适用范围从语言推理扩展到基于扩散的视觉生成和基于流的生成。然而，针对扩散生成大语言模型的高效RL系统仍未得到充分探索。现有实现（如veRL-Omni）仍依赖同地执行，这虽然简化了同步，但耦合了生成与训练资源、限制了异构部署，并制约了独立扩展。为此，我们提出DigenRL——一种面向扩散生成大语言模型的分布式RL框架，支持灵活资源分配、适配异构GPU并促进高效任务调度。为最大程度减少分布式架构中的执行气泡，我们提出：1）扩散架构中的生成轴流水线（GAP）和时间步并行（TSP），以实现生成与训练之间更细粒度的流水线；2）弹性训练器辅助生成（TAG）方法，使训练器GPU资源能够动态辅助执行生成；3）紧一步约束的异步策略，以进一步利用流水线中的尾部气泡。我们在三个硬件测试台上使用HunyuanVideo-13B、Wan2.1-14B、FLUX.1-12B和QwenImage-20B生成模型进行了大量实验，实验结果表明，DigenRL相比最先进的扩散RL系统veRL-Omni和GenRL实现了1.56-2.10倍的吞吐量提升。

## Abstract
Reinforcement learning (RL) has become a dominant post-training paradigm, driving the emergence of high-performance RL systems such as veRL for autoregressive large language models (LLMs). In parallel, diffusion-oriented RL algorithms, e.g., DanceGRPO and FlowGRPO, have rapidly expanded the scope of RL from language reasoning to diffusion-based visual and flow-based generation. However, efficient RL systems for diffusion generative LLMs remain underexplored. Existing implementations, e.g., veRL-Omni, still rely on colocated execution, which simplifies synchronization but couples rollout and training resources, limits heterogeneous deployment, and constrains independent scaling.   To this end, we introduce DigenRL, a disaggregated RL framework for diffusion-based generative LLMs that supports flexible resource allocation, accommodates heterogeneous GPUs, and facilitates efficient task scheduling. To maximally reduce the execution bubbles in the disaggregated architecture, we propose: 1) a generation-axis pipeline (GAP) and time-step parallelism (TSP) in the diffusion architecture to enable finer-grained pipelining between rollout and training; 2) an elastic trainer-assisted generation (TAG) approach to enable the trainer GPU resources to dynamically assist in executing rollout generations; and 3) a tightly one-step constrained asynchronous strategy to further utilize the tail bubble in the pipeline. Extensive experiments are conducted on three hardware testbeds with 16-32 GPUs using HunyuanVideo-13B, Wan2.1-14B, FLUX.1-12B, and QwenImage-20B generative models. Experimental results show that DigenRL achieves 1.56-2.10x throughput improvements over state-of-the-art diffusion RL systems, veRL-Omni and GenRL.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：强化学习（RL）已成为大语言模型（LLM）后训练的主流范式，针对自回归LLM的高性能RL系统（如veRL）已较为成熟。同时，扩散导向的RL算法（如DanceGRPO、FlowGRPO）将RL的应用从语言推理扩展至扩散视觉生成和基于流的生成。
- **核心问题**：现有针对扩散生成大语言模型的RL系统（如veRL-Omni）仍采用“同地执行”（colocated execution）架构，即生成（rollout）与训练资源耦合在同一组GPU上。这种设计虽然简化了同步，但导致：
  - 生成与训练资源无法独立扩展；
  - 难以支持异构GPU部署；
  - 限制了独立缩放（independent scaling）和灵活的任务调度。
- **整体含义**：面向扩散生成LLM的高效RL系统尚未得到充分探索，亟需一种解耦（disaggregated）架构，以实现资源灵活分配、异构GPU适配和高效调度。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**DigenRL**——面向扩散生成大语言模型的分布式RL框架，采用解耦架构，将生成与训练资源分离，实现灵活资源分配、异构GPU支持和高效任务调度。为最小化解耦架构中的执行气泡（execution bubbles），提出三项关键技术：

### 2.1 生成轴流水线（GAP）和时间步并行（TSP）
- **GAP**：在扩散架构中引入生成轴流水线，将生成（rollout）过程沿生成轴进行流水线切分，使生成与训练能更细粒度地重叠执行。
- **TSP**：时间步并行，在扩散模型的时间步（denoising steps）维度上实现并行，进一步流水线化生成与训练阶段。

### 2.2 弹性训练器辅助生成（TAG）
- **TAG**：允许训练器GPU资源动态地辅助执行生成任务。当训练器空闲或有富余算力时，可临时参与生成，从而减少生成等待时间，消除部分气泡。

### 2.3 紧耦合一步约束的异步策略
- **异步策略**：在流水线尾部，利用“紧一步约束”（tightly one-step constrained）的异步更新，进一步填充流水线末尾的气泡，最大化资源利用率。

- **算法流程说明**（基于摘要推断）：
  - 整体采用解耦的“生成器-训练器”分离架构；
  - 扩散生成任务通过GAP和TSP拆分为多个微阶段，生成器与训练器以流水线方式执行；
  - 训练器在空闲时通过TAG机制动态接管部分生成步骤；
  - 采用异步策略控制更新频率，确保训练与生成之间保持紧耦合的一步约束，避免过时梯度。

## 3. 实验设计：数据集/场景、基准、对比方法

- **测试模型**：使用了四种生成模型：
  - HunyuanVideo-13B
  - Wan2.1-14B
  - FLUX.1-12B
  - QwenImage-20B
- **硬件测试台**：三个不同的硬件平台，使用16-32块GPU。
- **对比方法**：
  - veRL-Omni（最先进的扩散RL系统之一）
  - GenRL（另一种扩散RL系统）
- **评估指标**：吞吐量提升倍数。
- **实验结果**：DigenRL相比veRL-Omni和GenRL实现了**1.56-2.10倍**的吞吐量提升。

## 4. 资源与算力

- 文中明确说明：实验在**16-32块GPU**上完成，使用了三个硬件测试台。但未具体说明GPU型号（如A100、H100等）以及训练时长（小时/天）。仅提及“16-32 GPUs”，具体型号和时长未提供。

## 5. 实验数量与充分性

- **实验数量**：文中提到“大量实验”（Extensive experiments），覆盖了四种不同规模的模型（12B-20B参数），并在三个不同硬件平台、16-32 GPU配置下进行对比。但未列出详细的消融实验或不同组件（GAP、TSP、TAG、异步策略）单独的效果验证。
- **充分性与公平性**：
  - 优点：对比了两种现有最强系统，覆盖多种模型与硬件，结果统一显示提升。
  - 不足：缺乏对每个技术贡献的消融分析（例如，去除TAG或去除TSP后的性能），也未说明是否使用相同RL算法（如GRPO）和超参数设置，难以判断公平性。实验的统计显著性（多次重复、误差棒）未提及。

## 6. 论文的主要结论与发现

- **结论**：DigenRL验证了解耦架构在扩散RL中的有效性，能够实现灵活资源分配与高效调度。
- **发现**：相比同地执行系统，吞吐量提升1.56-2.10倍；解耦设计支持异构GPU部署和独立扩展，解决了现有系统的关键局限。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首次将解耦架构系统性地应用于扩散生成LLM的RL训练，提出GAP、TSP、TAG和异步策略组合，有效减少分布式气泡。
  - TAG方法利用训练器空闲资源动态辅助生成，具有弹性调度优势。
  - 异步策略设计紧耦合约束，平衡了效率与收敛稳定性（推测）。
- **实验亮点**：
  - 覆盖从12B到20B参数的多种主流扩散生成模型（视频、图像、流式），场景多样。
  - 在三个不同硬件测试台进行验证，增强了结果的可信度。

## 8. 不足与局限

- **实验覆盖不足**：
  - 未提供消融实验，无法量化各技术组件（GAP、TSP、TAG、异步）的贡献大小。
  - 未与更多基线（如原始veRL、端到端同地配置）对比，仅对比veRL-Omni和GenRL。
  - 缺少收敛性分析（训练损失、奖励曲线等），只报告了吞吐量。
- **偏差风险**：
  - 未说明是否使用相同RL算法（如GRPO）进行公平对比；若基线系统使用不同RL算法，则吞吐量对比可能不平行。
  - 未讨论解耦架构对模型收敛质量的影响（如是否因异步更新导致性能下降）。
- **应用限制**：
  - 仅验证了16-32 GPU规模，更大规模下的扩展性未知。
  - 未涉及实际部署中的通信瓶颈、容错恢复等工程细节。
  - 仅针对扩散生成模型，是否可推广至其他生成范式（如自回归生成）未讨论。

（完）
