---
title: Improving Visual Representation Alignment Generation with GRPO
title_zh: 利用GRPO改进视觉表示对齐生成
authors: "Shentong Mo, Sukmin Yun"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00583v1"
tags: ["query:image-gen-rl"]
score: 8.0
evidence: 采用强化学习优化扩散变换器中的视觉表示对齐
tldr: 扩散Transformer训练中生成与判别表示对齐弱，现有REPA的静态对齐损失缺乏自适应性。提出VRPO，将表示对齐视为奖励引导过程，根据生成保真度、感知质量和语义连贯性给予自适应奖励。在ImageNet-256x256上相比REPA，FID提升1.8，训练加速2.3倍。该方法无缝集成到扩散Transformer，计算开销可忽略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00583v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1072, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00583v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1218, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00583v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1212, \"height\": 575, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00583v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00583v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 234, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00583v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1044, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00583v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 546, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00583v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 938, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00583v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 816, \"height\": 201, \"label\": \"Table\"}]"
motivation: 现有REPA的静态对齐损失无法动态平衡表示一致性与生成质量，导致判别收益有限。
method: 提出VRPO，用强化学习奖励引导对齐，根据生成图像的多维质量指标自适应调整对齐约束。
result: 在ImageNet-256x256上，VRPO相比REPA实现FID提升1.8和2.3倍训练加速。
conclusion: VRPO有效提升扩散Transformer的收敛速度与生成保真度，且几乎不增加计算成本。
---

## 摘要
最近的扩散变换器展示了强大的图像合成能力，但由于生成表示与判别表示之间对齐不足，训练效率仍然较低。虽然REPA等表示对齐框架通过将含噪去噪特征与预训练视觉编码器对齐来改善收敛，但其外部监督的对齐损失是静态的，在训练和推理过程中缺乏自适应性。现有方法依赖于固定的余弦对齐或对比目标，无法动态平衡表示一致性与生成质量，导致判别收益有限，且未能以任务自适应方式优化对齐。为了解决这一问题，我们提出了VRPO，一种基于强化学习的优化策略，用生成表示策略优化目标替代REPA的静态对齐损失。VRPO不对齐施加固定的相似性约束，而是将表示对齐视为奖励引导的过程：模型根据扩散特征与预训练视觉嵌入之间的生成保真度、感知质量和语义一致性获得自适应奖励。这一形式使生成器能够持续将内部表示向语义有意义的方向优化，同时提升图像质量。我们的VRPO驱动训练无缝集成到扩散变换器中，引入可忽略的计算开销，并与SiT和DiT架构完全兼容。在ImageNet-256x256上的大量实验表明，我们的VRPO-对齐显著提升了收敛速度和保真度，在相同计算预算下，相比REPA实现了高达+1.8 FID的改进和2.3倍的训练加速。

## Abstract
Recent diffusion transformers have demonstrated strong image synthesis capabilities but remain inefficient to train due to weak alignment between generative and discriminative representations. While representation alignment frameworks such as REPA improve convergence by aligning noisy denoising features with pretrained visual encoders, their externally supervised alignment loss is static and lacks adaptivity during training and inference. Existing methods rely on fixed cosine alignment or contrastive objectives, which cannot dynamically balance representation consistency and generation quality, resulting in limited discriminative benefit and failing to optimize alignment in a task-adaptive manner. To address this, we propose VRPO, a reinforcement-based optimization strategy that replaces REPA's static alignment loss with a generative representation policy optimization objective. Instead of enforcing a fixed similarity constraint, VRPO treats representation alignment as a reward-guided process: the model receives adaptive rewards based on generation fidelity, perceptual quality, and semantic coherence between the diffusion features and pretrained visual embeddings. This formulation enables the generator to continuously refine its internal representations toward semantically meaningful directions while improving image quality. Our VRPO-driven training seamlessly integrates into diffusion transformers, introducing negligible computation cost and preserving full compatibility with SiT and DiT architectures. Extensive experiments on ImageNet-256x256 demonstrate that our VRPO-Alignment substantially enhances both convergence and fidelity, achieving up to +1.8 FID improvement and 2.3x faster training compared to REPA under identical compute budgets.

---

## 论文详细总结（自动生成）

### 详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：扩散变压器（Diffusion Transformer, DiT/SiT）在图像生成中表现出强大的可扩展性和保真度，但训练计算成本高、样本效率低，主要原因是生成表示与判别表示之间的对齐较弱。现有工作（如 REPA）通过引入外部预训练视觉编码器（如 DINOv2）的余弦对齐损失来改善收敛，但这种对齐是**静态的**：它使用固定的相似性约束，在训练和推理过程中缺乏自适应性，无法根据当前生成质量动态调整对齐强度。  
- **核心问题**：静态对齐损失可能导致**过度约束**（生成保真度下降）或**约束不足**（语义一致性不足），且无法利用感知质量反馈（如 FID、LPIPS）来引导优化，因此难以平衡判别一致性与生成真实性。  
- **研究意义**：需要一种**动态的、奖励驱动的优化框架**，使扩散变压器能在训练中自适应地平衡语义对齐与生成质量，同时不引入额外网络或架构修改。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将表示对齐问题重新定义为**强化学习策略优化问题**。扩散变压器视为随机策略 πθ，通过最大化组合奖励来优化参数，从而替代 REPA 的静态余弦对齐损失。  
- **关键技术细节**：  
  - **奖励分解**：总奖励 R = α·R_fidelity + β·R_semantic + γ·R_stability（α+β+γ=1）  
    - **保真度奖励** R_fidelity：衡量生成图像的感知质量（基于 FID 代理和 LPIPS）  
    - **语义奖励** R_semantic：扩散特征 ht 与预训练视觉编码器（DINOv2）嵌入之间的余弦相似度（动态替代 REPA 的静态对齐损失）  
    - **稳定性奖励** R_stability：约束相邻时间步特征间的 L2 距离，防止表示漂移  
  - **策略优化**：使用带自适应重要性权重的策略梯度：  
    ∇θ J(θ) = E[R(x₀) ∇θ log πθ(x₀|x̃_t, t)]  
    归一化奖励 R' = (R – μR)/(σR + ε)，最终 VRPO 损失 L_VRPO = – E[w(θ) R(x₀)]  
  - **自适应学习率调度**：η 根据奖励方差动态调整，早期强调语义对齐，后期聚焦细节重建。  
  - **分层梯度注入**：仅对早期至中期 transformer 层（语义块）应用 VRPO 梯度，后期层（纹理块）保留标准去噪梯度，避免过度约束。  
  - **推理时对齐持续存在**：由于对齐已融入学习策略，推理时无需额外模块。

#### 3. 实验设计：数据集、基准、对比方法
- **主要数据集**：ImageNet-256×256（类条件图像生成）  
- **评估指标**：FID（↓）、Inception Score（↑）、CLIP-Score（↑）、表示对齐度 RA（↑ = 扩散特征与 DINOv2 的余弦相似度）  
- **对比方法**：  
  - 基线：DiT、SiT（无对齐）  
  - 对齐方法：REPA（静态余弦对齐）、REG（带时间一致性正则化）  
- **额外实验**：跨域迁移到 CIFAR-10 和 CelebA-HQ（无微调），验证泛化性。

#### 4. 资源与算力
- 论文明确提到：使用 **8×A100 GPU**，batch size 256，优化器 AdamW（β₁=0.9, β₂=0.999），学习率 1×10⁻⁴ 余弦衰减，总训练迭代约 **400K** 次收敛。  
- 训练效率：VRPO 约在 **170K** 次迭代达到 FID 4.12，相比 REPA 加速 **2.3×**。  
- 额外开销：每次迭代仅增加约 **3%** 训练时间（奖励计算依赖前向缓存的输出和冻结编码器）。

#### 5. 实验数量与充分性
- **实验组数**：包括 1 个主要对比表（6 方法）、3 个消融实验（奖励组件、层对齐策略、奖励权重敏感性）、2 个跨域迁移表、1 个收敛速度比较表、1 个定性可视化分析（图 2、图 3）。附录中还有理论证明和额外分析。  
- **充分性与公平性**：  
  - 对比方法均使用相同设置（batch size、A100 GPU、训练时长等），REPA 和 REG 代码公开且基线为 DiT/SiT，比较公平。  
  - 消融实验覆盖了奖励三要素、层梯度注入位置、权重配比，验证了各组件贡献。  
  - 跨域迁移实验证明了表示的可泛化性，但仅在两个小数据集上测试，泛化范围有限。  
  - 所有实验均报告 FID、IS、CLIP-Score、RA 等多维指标，客观全面。  
  - 但缺少与基于 RL 的其他图像生成优化方法（如 ImageReward、Flow-GRPO）的直接对比，仅在相关工作提及。

#### 6. 论文的主要结论与发现
- VRPO 在 **ImageNet-256×256** 上相比 REPA 实现 **+1.8 FID 提升**（5.93→4.12）和 **+3.1 RA 提升**（0.70→0.76），训练速度 **2.3× 加速**。  
- **语义奖励**是表示对齐的主要驱动力，**保真度奖励**维持感知质量，**稳定性奖励**减少训练振荡。三者协同最优。  
- **早期层对齐**策略效果最佳：仅对前 25%~50% 层应用奖励梯度，平衡对齐与细节保留。  
- 跨域迁移保持 **92% 以上 RA**，证明表示具有较强鲁棒性和可迁移性。  
- 推理时对齐自动持续，无需额外计算。

#### 7. 优点
- **创新性**：首次将强化学习用于扩散模型的**内部表示对齐**，替代静态损失，实现自适应动态优化。  
- **无需额外网络**：仅使用冻结的 DINOv2 编码器，兼容现有 DiT/SiT 架构，计算开销极低。  
- **多层奖励设计**：同时考虑生成保真度、语义一致性和时间稳定性，理论完备（提供收敛性、Pareto 最优性等证明）。  
- **实用性强**：训练加速显著（2.3×），且推理阶段对齐自动保持，适合大规模训练部署。  
- **实验设计全面**：包括主要对比、消融、迁移、收敛曲线、可视化，评估客观。

#### 8. 不足与局限
- **依赖预训练编码器**：语义奖励基于 DINOv2，其领域偏向可能限制在医疗、卫星等特殊域的效果。  
- **保真度奖励需要 EMA 代理**：因 FID 无法逐批次计算，需维护实时样本特征统计，增加少量 VRAM 开销。  
- **缺少与 SOTA 图像级 RL 方法的直接比较**：如 Flow-GRPO 等直接在生成图像上使用奖励的方法，论文未作对比。  
- **跨域迁移仅在小数据集测试**：CIFAR-10 和 CelebA-HQ 与 ImageNet 差异有限，更复杂域（如纹理丰富、抽象）未验证。  
- **潜在滥用风险**：高语义一致性可能使深度伪造更容易，论文建议未来加入负奖励抑制不安全内容，但未实现。  
- **理论分析部分未完全实证**：虽然提供了六个定理，但未在实验中量化解耦每个定理的效果，如信息瓶颈解释等。

（完）
