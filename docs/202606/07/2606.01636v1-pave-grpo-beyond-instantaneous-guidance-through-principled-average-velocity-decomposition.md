---
title: "Pave-GRPO: Beyond Instantaneous Guidance through Principled Average Velocity Decomposition"
title_zh: Pave-GRPO：通过原则性平均速度分解超越瞬时引导
authors: "Pengyang Ling, Jiazi Bu, Yujie Zhou, Yibin Wang, Zhenyu Hu, Zihan Zhang, Yi Jin, Huaian Chen, Yuhang Zang"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01636v1"
tags: ["query:image-gen-rl"]
score: 9.0
evidence: 使用组相对策略优化对齐基于流的生成模型
tldr: 现有GRPO后训练中，流模型因生成成本限制只能使用极少数去噪步数，导致奖励反馈仅覆盖少量阶段，偏好对齐不充分。本文提出Pave-GRPO，通过原则性平均速度分解，将每次粗步进等价拆解为多个中间时间步的细子轨迹，在不增加生成成本下使奖励信号传播到更密集的时间阶段。该方法实现零成本视界扩展与全面时间监督，显著提升偏好对齐效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1849, \"height\": 1078, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 196, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01636v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1832, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01636v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1284, \"height\": 1383, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01636v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1852, \"height\": 303, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01636v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1680, \"height\": 883, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01636v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1696, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01636v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1753, \"height\": 267, \"label\": \"Table\"}]"
motivation: GRPO后训练受限于少步去噪，奖励反馈稀疏，对齐不充分。
method: 通过平均速度分解，将粗过渡等价为多时间步子轨迹，密集化奖励监督。
result: 无需额外生成成本，扩展优化视界，提升偏好对齐性能。
conclusion: Pave-GRPO为流模型偏好对齐提供高效密集监督方案，效果显著。
---

## 摘要
基于群组相对策略优化（GRPO）的后训练已成为将基于流的生成模型与人类偏好对齐的强大范式。然而，流模型的迭代去噪特性在生成用于策略梯度更新的群组展开时会产生巨大成本，迫使现有方法使用极少的去噪步骤进行训练。这种时间稀疏性严重限制了偏好优化：奖励反馈只能到达每条轨迹的少数阶段，使绝大多数中间去噪步骤缺乏直接监督，从而损害了对齐的粒度。为解决这一问题，我们提出Pave-GRPO，通过原则性平均速度分解重新表述GRPO目标。我们并非生成高成本的多步展开，而是保持高效的少步群组采样，但将每个粗粒度过渡分解为覆盖多个中间时间步的等效精细子轨迹集合。这将奖励反馈传播到更密集的时间阶段集，从而实现更全面的偏好对齐，且不增加生成成本。该设计带来两个优势：(i) 零成本视野扩展：通过直接重用分片段群组样本及其相关奖励，Pave-GRPO在固定采样预算下显著拓宽了有效优化范围；(ii) 全面的时间监督：通过将瞬时速度目标等效分解为多时间步集合，它将奖励信号分布到去噪过程的更多中间阶段，实现更细粒度和更彻底的偏好优化。大量实验验证，Pave-GRPO在不同奖励设置下有效推进了偏好对齐，提供了全面的性能提升。

## Abstract
Post-training via Group Relative Policy Optimization (GRPO) has emerged as a powerful paradigm for aligning flow-based generative models with human preferences. However, the iterative denoising nature of flow models incurs substantial costs when generating group rollouts for policy-gradient updates, compelling existing methods to train with extremely few denoising steps. This temporal sparsity severely restricts preference optimization: reward feedback can only reach a handful of stages per trajectory, leaving the vast majority of intermediate denoising steps without direct supervision and thus compromising alignment granularity. To address this, we propose Pave-GRPO, which reformulates the GRPO objective through Principled average velocity decomposition. Rather than generating expensive high-step rollouts, we maintain efficient few-step group sampling but decompose each coarse transition into an equivalent ensemble of finer sub-trajectories spanning multiple intermediate timesteps. This propagates reward feedback to a denser set of temporal stages for more comprehensive preference alignment without additional generation cost. This design offers two benefits: (i) zero-cost horizon expansion: through the direct reuse of piece-wise group samples and their associated rewards, Pave-GRPO significantly broadens the effective optimization scope under fixed sampling budgets; and (ii) comprehensive temporal supervision: by equivalently decomposing an instantaneous velocity target into a multi-timestep ensemble, it distributes reward signals across more intermediate stages of the denoising process, enabling finer-grained and more thorough preference optimization. Extensive experiments validate that Pave-GRPO effectively advances preference alignment across different reward settings, offering comprehensive performance enhancement.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：Group Relative Policy Optimization (GRPO) 已被用于对基于流的生成模型进行后训练，以使其对齐人类偏好。然而，流模型的迭代去噪过程在生成策略梯度更新所需的群组 rollout 时成本极高。
- **核心问题**：现有方法为了控制生成成本，被迫使用极少数去噪步骤进行训练（如 16 步）。这导致**奖励反馈只能覆盖每条轨迹的稀疏时间阶段**，大部分中间去噪步骤缺乏直接监督，严重限制了偏好对齐的细粒度。
- **整体含义**：作者指出，**生成步数预算与优化粒度之间存在矛盾**：为了成本而降低生成步数，却牺牲了奖励信号在时间轴上的传播密度。本文旨在打破这一矛盾，在不增加生成成本的前提下实现更密集的时间监督。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- **原则性平均速度分解（Principled Average Velocity Decomposition）**：利用去噪过程的马尔可夫性质以及 ODE/SDE 共享可解析高斯概率路径的特性，将一次粗粒度的纯 SDE 过渡等价分解为多条更细的子轨迹（混合 ODE–SDE 路径）。这些子轨迹共享同一个终点状态和相同的奖励优势，从而将奖励信号反向传播到更多的中间时间步。
- **关键洞察**：
  - 去噪过程是马尔可夫链，中间状态可作为后续轨迹的充分统计量。
  - 纯 SDE 和混合 ODE–SDE 路径产生的状态均服从高斯分布，其似然可直接计算，因此无需额外采样即可计算重要性采样比。

### 关键技术细节
- **混合路径构造**：对于从时间 \(t_i\) 到 \(t_{i+1}\) 的一次粗过渡（纯 SDE），构造多条 \(K\) 步的混合 ODE–SDE 路径（例如先做 \(K-1\) 步 ODE，最后一步用 SDE）。该路径终点 \(x_{t_{i+1}}\) 的分布概率可解析表达。
- **重要性采样比**：对于每条混合路径，计算其相对于旧策略的密度比 \(\eta_{i,t,k}(\theta)\)，并与原纯路径的密度比 \(\rho_{i,t}(\theta)\) 一起使用，共享同一个组内归一化优势 \(\hat{A}_i\)。
- **训练目标**（公式 (10)）：
  \[
  J_{\text{Pave-GRPO}}(\theta) = \mathbb{E}\left[ \frac{1}{GT} \sum_{i=1}^{G} \sum_{t=1}^{T} \left( \text{Surr}(\rho_{i,t}(\theta), \hat{A}_i) + \frac{1}{|S|} \sum_{k \in S} \text{Surr}(\eta_{i,t,k}(\theta), \hat{A}_i) - \beta D_{\text{KL}} \right) \right]
  \]
  其中 \(S\) 是分解因子集合（如 \(\{2,3\}\)），\(\text{Surr}\) 为 clipped surrogate 函数。
- **零成本扩展**：因为直接使用已有的组样本及其奖励，无需额外采样，所以优化视野被有效扩展。

## 3. 实验设计：数据集/场景、基准、对比方法

### 数据集
- **训练与测试**：HPD 数据集（>10 万提示），留出 400 提示做测试。
- **额外评估**：UniGenBench（600 提示，10 个语义维度：风格、知识、属性、动作、关系、复合、语法、推理、布局、文字）。

### 基准与对比方法
- 骨干模型：Flux.1-dev（12B 参数）。
- 对比方法：
  - Flow-GRPO
  - Dance-GRPO
  - Flow-CPS
- 本方法提供两个变种：Pave-GRPO (F_mar_sde) 和 Pave-GRPO (F_snr_sde)。

### 评估指标
- **CLIP**（图像-文本对齐）。
- **奖励模型**：ImageReward, HPS-v2, HPS-v3, UnifiedReward-v1/v2, Pick-Score。
- **UniGenBench 的 MLLM 评估**：10 个维度分数及总体分。

### 训练设置
- 全参数训练，batch size=8，AdamW 优化器，lr=2e-6，bfloat16 混合精度。
- 训练步数：16 步（纯 SDE 采样在步 0,2,4,6 进行），推理时用 50 步。
- 随机性强度 \(\sigma_t\) 依据 SDE 类型设置。
- 组大小 \(G=12\)，采用 step-wise reward（来自 Flow-GRPO 变体）。

## 4. 资源与算力

论文中**未明确说明使用的 GPU 型号、数量及总训练时长**。仅提及：
- 使用 bfloat16 混合精度进行训练加速。
- 在消融实验中给出了每轮迭代时间（从 71s 到 79s，取决于分解因子集合 \(S\)），但未注明具体硬件。

因此，无法获得准确的算力描述。

## 5. 实验数量与充分性

- **实验组数**：
  - **4 类奖励设置**：HPS-v2、HPS-v3、HPS-v2+CLIP、HPS-v3+CLIP。
  - 每种设置下对比了 4 种基线 + 2 种本方法变体，共约 24 个测量点（表 1）。
  - **UniGenBench 评估**：10 个维度 + 总体，比较 4 种方法（表 2）。
  - **消融实验**：逐步增加分解因子 \(S = \{2\}, \{2,3\}, \{2,3,4\}, \{2,3,4,5\}\)，对比 7 个指标和每轮时间（表 3）。
  - **训练曲线**：图 5 展示了 Flow-GRPO 与 Pave-GRPO 在两种奖励设置下的训练过程。
- **充分性与公平性**：实验设计较为全面，覆盖多种奖励模型和基准，对比方法采用相同训练配置（步数、组大小、SDE 类型等）。消融实验系统分析了分解因子的影响。结论有定量和定性（图 4）支撑。

## 6. 论文的主要结论与发现

- **主要结论**：Pave-GRPO 在不增加生成成本的前提下，通过等效轨迹分解将奖励信号传播到更密集的时间阶段，显著提升了偏好对齐效果。
- **关键发现**：
  - 在所有奖励设置下，Pave-GRPO 在 in-domain 和 out-of-domain 指标上一致优于现有方法。
  - 在 UniGenBench 上，Pave-GRPO (F_mar_sde) 以 63.96 总体分刷新 SOTA，尤其在**属性绑定**（+2.03）、**文本渲染**（+4.31）等维度提升显著。
  - 两种 SDE 变体均有效，验证了方法对路径构造的鲁棒性。
  - 分解因子 \(S = \{2,3\}\) 在性能与效率之间取得最佳平衡；增大 \(S\) 收益递减且计算开销增加。

## 7. 优点

- **零成本视野扩展**：通过直接重用组样本及其奖励，无需额外采样即可扩大优化跨度。
- **细粒度时间监督**：将单步瞬时速度等价分解为多步子速度，让奖励信号覆盖更多中间步骤。
- **方法通用性**：不依赖于特定 SDE 类型（marginal-preserving 或 SNR-preserving），可跨不同路径构造迁移。
- **实验充分**：涵盖多种奖励模型、多个基准、消融实验和定性展示，验证了泛化能力和稳定性。

## 8. 不足与局限

- **计算开销增加**：虽然不增加采样成本，但混合路径需要额外的前向传播计算，导致每轮训练时间随分解因子增加而上升（从 71s 到 79s）。
- **边际收益递减**：过细分解（如 \(S\) 包含 4,5）带来性能提升有限，却显著增加计算负担。
- **骨干模型单一**：仅在 Flux.1-dev 上测试，未扩展到其他流模型（如 Stable Diffusion 3, HunyuanVideo 等），泛化性需要进一步验证。
- **仅限文本到图像**：方法未在视频或其他模态上验证，应用范围有限。
- **训练设置固定**：仅采用 16 步训练和 50 步推理，未探索更少或更多步数下的表现。
- **偏差风险**：奖励模型可能存在系统偏差，论文未讨论对齐过程可能放大这些偏差。

（完）
